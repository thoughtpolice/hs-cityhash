The cbits/ directory includes an empty config.h used by city.cc

This is supposed to be generated by autoconf, but it seems as if
it doesn't use any of the variables at all right now, so duping
it is fine.
